spring:
  application:
    name: flooding-server-v2

  datasource:
    url: ${DB_URL}
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}
    driver-class-name: ${DB_DRIVER:org.postgresql.Driver}

  jpa:
    hibernate:
      ddl-auto: ${JPA_DDL_AUTO:update}
    properties:
      hibernate:
        dialect: ${JPA_DIALECT:org.hibernate.dialect.PostgreSQLDialect}

  data:
    redis:
      host: ${REDIS_HOST:localhost}
      port: ${REDIS_PORT:6379}

management:
  endpoints:
    web:
      exposure:
        include: health
  endpoint:
    health:
      show-details: never

sdk:
  logging:
    enabled: true
    not-logging-urls:
      - "/v3/api-docs/**"
      - "/swagger-ui/**"

  response:
    enabled: true
    not-wrapping-urls:
      - "/v3/api-docs/**"
      - "/v3/api-docs"
      - "/swagger-ui/**"

  swagger:
    enabled: true
    title: "Flooding Server API"
    version: "v2"
    paths-to-match:
      - "/**"

  exception:
    enabled: true
    use-english-message: false

study:
  open-time: "${STUDY_OPEN_TIME:20:00}"
  close-time: "${STUDY_CLOSE_TIME:21:00}"
  max-count: ${STUDY_MAX_COUNT:50}
  lock-key: ${STUDY_LOCK_KEY:study:lock}

massage:
  open-time : "${MASSAGE_OPEN_TIME:20:20}"
  max-count : ${MASSAGE_MAX_COUNT:5}
  lock-key: ${MASSAGE_KEY:lock}

oauth:
  client-id: ${OAUTH_CLIENT_ID}
  client-secret: ${OAUTH_CLIENT_SECRET}

neis:
  openapi:
    timetable:
      baseUrl: ${NEIS_OPENAPI_TIMETABLE_BASE_URL:https://open.neis.go.kr/hub/}
      apiKey: ${NEIS_TIMETABLE_API_KEY}
      path: "hisTimetable"

datagsm:
  open-api-key: ${DATAGSM_OPEN_API_KEY}
  neis:
    meals:
      baseUrl: ${DATAGSM_NEIS_MEALS_BASE_URL:https://openapi.datagsm.kr/}
      apiKey: ${DATAGSM_NEIS_MEALS_API_KEY}
      path: "v1/neis/meals"

ai:
  chatbot:
    base-url: ${AI_CHATBOT_URL:http://localhost:8081}
  song:
    base-url: ${AI_SONG_URL:http://localhost:8082}

jwt:
  secret: ${JWT_SECRET}
  access-token-expiration: ${JWT_ACCESS_EXPIRATION:3600000}
  refresh-token-expiration: ${JWT_REFRESH_EXPIRATION:604800000}
